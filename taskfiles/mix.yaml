version: "3"

tasks:
  compile-force:
    dir: "{{.USER_WORKING_DIR}}"
    aliases: [mcf]
    cmds:
      - mix compile --force

  deps.get:
    dir: "{{.USER_WORKING_DIR}}"
    aliases: [mdg]
    cmds:
      - test -d deps && rm -f deps/* &>/dev/null || true
      - mix deps.get

  format:
    dir: "{{.USER_WORKING_DIR}}"
    aliases: [mf]
    cmds:
      - mix format

  phx.server:
    dir: "{{.USER_WORKING_DIR}}"
    aliases: [mps]
    cmds:
      - mix phx.server

  test:
    dir: "{{.USER_WORKING_DIR}}"
    aliases: [mt]
    cmds:
      - mix test

  iex-mix:
    dir: "{{.USER_WORKING_DIR}}"
    aliases: [im]
    cmds:
      - iex -S mix

  iex-phx.server:
    dir: "{{.USER_WORKING_DIR}}"
    aliases: [imps]
    cmds:
      - iex -S mix phx.server
